<!DOCTYPE html>
<html lang="en">

<head>
    <!-- basic -->
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <!-- mobile metas -->
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="viewport" content="initial-scale=1, maximum-scale=1">
    <!-- site metas -->
    <title>sbs</title>
    <meta name="keywords" content="">
    <meta name="description" content="">
    <meta name="author" content="">
    <!-- bootstrap css -->
    <link rel="stylesheet" href="/public/css/bootstrap.min.css">
    <!-- style css -->
    <link rel="stylesheet" href="/public/css/style.css">
    <!-- Responsive-->
    <link rel="stylesheet" href="/public/css/responsive.css">
    <!-- fevicon -->
    <link rel="icon" href="/public/images/fevicon.png" type="image/gif" />
    <!-- Tweaks for older IEs-->
    <link rel="stylesheet" href="https://netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.css">
    <!-- Custom Styles -->
    <link rel="stylesheet" href="/public/css/style1.css">
    <link rel="stylesheet" href="/public/css/styles.css">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">

    <!-- Fonts & Icons -->
    <link rel="stylesheet"
        href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@48,400,0,0" />
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css">

    <!-- jQuery, Popper.js, and Bootstrap JavaScript -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.3/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
    <style>
        .navbar {

            flex-direction: row;
            align-items: center;
            padding: 2rem;
            justify-content: left;
        }

        .nav-item {
            width: 100%;
            margin-bottom: 0.5rem;
            padding-left: 1rem;
        }

        .nav-link {
            display: flex;
            align-items: center;
            justify-content: left;
            padding: 0.5rem 2rem;
            width: 100%;
        }

        .active-nav-item .nav-link {
            background-color: black;
            color: white;
            padding: 10px;
        }

        header {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            background-color: #fcfcfc;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 1rem 2rem; /* Adjust padding as needed */
        }

        /* Add styles for the main content area */
        .content {
            margin-top: 80px; /* Adjust margin to make content start below the header */
        }

        /* Adjust table styles to take full remaining width */
        table {
            width: 100%;
        }        
    </style>
</head>

<body class="main-layout">
    <!-- Add your body content here -->
    <!-- loader -->
    <div class="shadowNav" style="    
        z-index: 2;
        background-color: #fcfcfc;
        display: grid;
        top: 0;
        height: 8rem;
        grid-template-columns: 60% 40%;
    align-items: center;
        flex-direction: row;
        position: fixed;
        width: 100%;
        justify-content: left;
    border: 1px solid 1px solid rgb(230 230 230);
    
    ">
        <h6 style="   font-weight: 800;
                font-size: 1.5em;
                padding-left: 5rem;"> Staff Home
        </h6>
        <div style="display: flex;width: 100%;justify-content:center;">
            <div style="position: relative; width: 20vw; margin-right: 24rem;">
            </div>
        </div>
    </div>
    <div class="navbar theme-orange shadowNav " style="border: 1px solid 1px solid rgb(230 230 230);">
        <div>
            <div style="padding-left: 1rem;height: 13rem">
                <img src="/public/images/1.png" alt="" class="avatar">
                <h2 style="font-size: 0.8rem;color: #2d2d2d;font-weight: bold;width: 100%;padding-top: 3rem;">
                    Welcome back
                </h2>
            </div>
            <ul class="navbar-nav" style="display: flex; flex-direction: column; justify-content: center; align-items: center;">
                <li class="nav-item">
                    <a class="nav-link" href="/additems">Add Items<i class="fas fa-shopping-cart"></i></a>
                </li>
                <li class="nav-item" style="width: 100%; border-top-left-radius: 6px; border-bottom-left-radius: 6px;">
                    <a class="nav-link" href="/edititems">Edit Items<i class="fas fa-shopping-cart"></i></a>
                </li>
                <li class="nav-item" style="width: 100%; border-top-left-radius: 6px; border-bottom-left-radius: 6px;">
                    <a class="nav-link" href="/addsuppliers">Add Suppliers<i class="fas fa-shopping-cart"></i></a>
                </li>                
                <li class="nav-item" style="width: 100%; border-top-left-radius: 6px; border-bottom-left-radius: 6px;">
                    <a class="nav-link" href="/vieworders">Store Orders<i class="fas fa-shopping-basket"></i></a>
                </li>                
                <li class="nav-item">
                    <a class="nav-link" href="/login">Logout<i class="fas fa-sign-out-alt"></i></a>
                </li>
            </ul>                      
        </div>

    </div>
    <!-- end header inner -->
    <!-- end header -->
    <!-- Content Area -->
    <div class="main-content" style="margin:0rem;">

        <div class="shadowNav" style="    
        z-index: 2;
        background-color: #fcfcfc;
        display: grid;
        height: 11vh;
        grid-template-columns: 60% 40%;
  align-items: center;
        flex-direction: row;
        position: fixed;
        width: 100%;
        justify-content: left;
  border: 1px solid 1px solid rgb(230 230 230);
  
  ">
              <div style="display: flex;width: 100%;justify-content: center;">
                    <h6 style="   font-weight: 800;
                    font-size: 1.5em;
                    padding-left: 15rem;">Manage Store Orders
                    </h6>
              </div>
    </div>

    <div class="contact" style="margin-top: 50px; overflow-y: auto;">
        <div >
            <div class="row">

                <!-- Table for displaying and editing Supplier Orders -->
                <div class="col-md-12">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Supplier Name</th>
                                <th>Supplier Item Name</th>
                                <th>Quantity</th>
                                <th>Total Price</th>
                                <th>Order Status</th>
                                <th>Date of Order</th>
                                <th>Date Fulfilled</th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody id="orderTableBody">
                            <!-- Table body will be dynamically populated with orders -->
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
        
</div>
    


    <!-- Modal for Editing Orders -->
    <div class="modal fade" id="editModal" tabindex="-1" role="dialog" aria-labelledby="editModalLabel"
        aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="editModalLabel">Edit Order</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <!-- Edit form will be dynamically populated here -->
                </div>
                <div class="modal-footer">
                    <button type="button" class="subsci_btn" data-dismiss="modal">Close</button>
                    <button type="button" class="subsci_btn" id="submitEditBtn">Submit Edit</button>
                </div>
            </div>
        </div>
    </div>
    <!-- End Modal -->

    <!--  footer -->
    <!-- end footer -->

    <!-- JavaScript files-->

    <script>
        let allSupplierItems = [];
        let allSuppliers = [];
        document.addEventListener("DOMContentLoaded", function () {


            fetch("http://localhost:5358/api/suppliers")
                .then(response => {
                    if (!response.ok) {
                        throw new Error(`HTTP error! Status: ${response.status}`);
                    }
                    return response.json();
                })
                .then(suppliers => {
                    allSuppliers = suppliers;
                    fetch("http://localhost:5358/api/supplieritems")
                .then(response => {
                    if (!response.ok) {
                        throw new Error(`HTTP error! Status: ${response.status}`);
                    }
                    return response.json();
                })
                .then(supplierItems => {
                    // Add code here
                    allSupplierItems = supplierItems;
                    //*****************
            // Fetch orders from the server
            fetch("http://localhost:5358/api/supplierproductorders")
                .then(response => {
                    if (!response.ok) {
                        throw new Error(`HTTP error! Status: ${response.status}`);
                    }
                    return response.json();
                })
                .then(orders => {
                    // Populate the table with orders
                    const orderTableBody = document.getElementById("orderTableBody");

                    orders.forEach(order => {
                    const row = document.createElement("tr");

                    let skipFirst = true;  // Flag to skip the first key

                        for (const key in order) {
                            if (skipFirst) {
                                skipFirst = false;
                                continue;  // Skip the first key
                            }

                            const cell = document.createElement("td");
                            if (key === 'supplierId') {
                                // do something first
                                const supplier = allSuppliers.find(supp => supp.supplierId == order.supplierId);
                                cell.textContent = supplier.supplierName;
                            }else if(key === 'supplierItemId'){
                                // do something first
                                const item = allSupplierItems.find(it => it.supplierItemId == order.supplierItemId);
                                cell.textContent = item.itemName;
                            }else{
                                cell.textContent = order[key];
                            }
                            row.appendChild(cell);
                        }


                        // Add Edit button with onclick event
                        const editButton = document.createElement("button");
                        editButton.textContent = "Edit";
                        editButton.className = "subsci_btn";
                        editButton.addEventListener("click", function () {
                            // Call the function to handle order editing
                            editOrder(order);
                        });

                        const actionCell = document.createElement("td");
                        actionCell.appendChild(editButton);
                        row.appendChild(actionCell);

                        orderTableBody.appendChild(row);
                    });
                })
                .catch(error => {
                    console.error('Fetch error:', error);
                });
                    //*****************
                })
                .catch(error => {
                    console.error('Fetch error:', error);
                });
                })
                .catch(error => {
                    console.error('Fetch error:', error);
                });

        });

        // Function to handle order editing
        function editOrder(order) {
            // Populate the modal body with the edit form
            const editForm = document.createElement("form");
            Object.keys(order).forEach(key => {
                const input = document.createElement("input");
                input.type = "text";
                input.name = key;
                input.value = order[key];
                editForm.appendChild(input);
            });
            // Set up the modal for editing
            const modalBody = document.querySelector("#editModal .modal-body");
            modalBody.innerHTML = "";
            modalBody.appendChild(editForm);
            // Set up the submit button in the modal
            const submitButton = document.getElementById("submitEditBtn");
            submitButton.onclick = function () {
                // Call the function to submit the edited order
                submitEdit(order.productOrderId, editForm);
            };
            // Show the modal
            $('#editModal').modal('show');
        }

        // Function to submit the edited order
        function submitEdit(orderId, editForm) {
            // Get edited data from the form
            const formData = new FormData(editForm);

            function convertToDataType(value) {
                if (!isNaN(value)) {
                    // If it's a number
                    return parseFloat(value);
                } else {
                    // If it's a date
                    const dateValue = new Date(value);
                    if (!isNaN(dateValue.getTime())) {
                        return value;
                    }
                }
                // Otherwise, return the original value
                return value;
            }

            const jsonData = {};
            formData.forEach((value, key) => {
                // Ensure the data type is preserved
                jsonData[key] = convertToDataType(value);
            });
            

            // Use fetch to submit the edited data to the specified URL
            fetch(`http://localhost:5358/api/supplierproductorders/${jsonData.productOrderId}`, {
                method: "PUT",
                headers: {
                    "Content-Type": "application/json",
                },
                body: JSON.stringify(jsonData),
            })
                .then(response => {
                    if (!response.ok) {
                        throw new Error(`HTTP error! Status: ${response.status}`);
                    }
                    //return response.json();
                })
                .then(data => {
                    // Handle the success response data if needed
                    console.log(data);

                    // Close the modal after successful submission
                    $('#editModal').modal('hide');
                })
                .catch(error => {
                    // Handle errors
                    console.error('Fetch error:', error);
                });
        }
    </script>
<script>
    document.addEventListener("DOMContentLoaded", function () {
        // Get all nav items
        const navItems = document.querySelectorAll('.nav-item');
        const navLinks = document.querySelectorAll('.nav-link');

        // Function to remove active class from all items
        function removeActiveClass() {
            navItems.forEach((innerItem) => innerItem.classList.remove('active-nav-item'));
        }

        // Add click event listener to each nav item
        navItems.forEach((item, index) => {
            item.addEventListener('click', function () {
                removeActiveClass();
                // Add active class to clicked item
                this.classList.add('active-nav-item');
            });
        });

        // Check current URL and set active class
        const currentURL = window.location.pathname;
        navLinks.forEach((link, index) => {
            if (link.getAttribute('href') === currentURL) {
                removeActiveClass();
                navItems[index].classList.add('active-nav-item');
            }
        });
    });

</script>    
</body>


</html>